#SPDX-License-Identifier: MIT-0
---
# tasks file for bind9
#
- name: Install bind9
  ansible.builtin.include_tasks:
    file: "install-{{ ansible_os_family | lower }}.yml"

- name: Configure bind9
  ansible.builtin.include_tasks:
    file: "configure-{{ ansible_os_family | lower }}.yml"
